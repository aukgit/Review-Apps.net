@model IEnumerable<ReviewApps.Models.POCO.Identity.ApplicationRole>
@{
    ViewBag.Title = "Role Management";
    string title = ViewBag.Title;
    string subtitle = ViewBag.Title;
}
@Html.SamePageLinkWithIcon(title, subtitle, "glyphicon glyphicon-th-list")
<hr />
<h2 class="h5">Lower priority means higher power.</h2>
@*<div class="role-left-stack top-margin-space">
    <a href="@Url.Action("Create")" title="Create a new item." class="btn btn-warning create-save"><span class="glyphicon glyphicon-plus"></span> Create Role</a>
</div>*@
<div class="top-margin-space"></div>
<table class="bootstrap-table-do table table-hover" data-search="true">
    <thead>
        <tr>
            <th data-field="id">
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PriorityLevel)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CanBeAcheivedByPoint)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PointsRequired)
            </th>
            @*
                <th></th>*@
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model) {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.PriorityLevel)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.CanBeAcheivedByPoint)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.PointsRequired)
                </td>
                @*<td>
                        @Html.ActionLink("Edit", "Edit", new {id = item.Id}) |
                        @Html.ActionLink("Delete", "Delete", new {id = item.Id}, new {onclick = "return confirm('Are you sure?')"})
                    </td>*@
            </tr>
        }
    </tbody>
</table>
@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}